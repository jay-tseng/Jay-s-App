<%- include('bootheader') -%>
<%- include('menubar') -%>
<img src="images/c.png" class="img-fluid" width="100%" >

    <div class="jumbotron">
        <h1>Discussion</h1>
        <p>Post your book Discussion under</p>
    </div>

    <form method="post" action="/forum">
      <h1>New Post: </h1><br>
      <!-- <textarea id="post" name="post"></textarea> -->
      <textarea class="form-control" id="post" rows="5"></textarea>
      <br>
      <!-- <input type="submit">  -->
      <input type="submit" class="btn btn-primary"></button>

    </form>
    <hr>
    <form method="post" action="/forumDelete">
        <input type="submit" value="delete marked">
        <ol>
        <% for(i = 0; i<posts.length; i++){  %>
            <li> <a href="/showPost/<%= posts[i]._id %>">
                  <%= posts[i].userName %></a>
                  <i>
                 <%= posts[i].createdAt %></\/i> <br>
                 <% console.dir([posts[i].userId,user._id]) %>
                 <% if (posts[i].userId.equals(user._id)) { %>
                     <input type="checkbox" name="delete" value="<%= posts[i]._id %>">
                 <% } %>
                 <pre><%= posts[i].post %></pre>

             </li>
        <% } %>
        </ol>

    </form>

<%- include('bootfooter') -%>
